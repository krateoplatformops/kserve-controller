apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kserve-controller.fullname" . }}
  labels:
  {{- include "kserve-controller.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }} 